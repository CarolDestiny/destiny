# Preserve audited bytes and vendored-source provenance across all platforms.
# Keep text diffs; accept CRLF endings and existing final blank lines.
* -text whitespace=blank-at-eol,space-before-tab,cr-at-eol,-blank-at-eof

# Pinned golden-output fixtures contain intentional trailing spaces.
thirdLib/googletest/googletest/test/*-golden-*.txt -whitespace
