{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: Configure (GCC x64 Debug)",
            "type": "process",
            "command": "cmake",
            "args": [
                "--preset",
                "gcc64-debug"
            ],
            "options": {
                "cwd": "${workspaceFolder}/code"
            },
            "problemMatcher": []
        },
        {
            "label": "CMake: Build (GCC x64 Debug)",
            "type": "process",
            "command": "cmake",
            "args": [
                "--build",
                "--preset",
                "gcc64-debug",
                "--parallel"
            ],
            "options": {
                "cwd": "${workspaceFolder}/code"
            },
            "dependsOrder": "sequence",
            "dependsOn": "CMake: Configure (GCC x64 Debug)",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "CMake: Clean (GCC x64 Debug)",
            "type": "process",
            "command": "cmake",
            "args": [
                "--build",
                "--preset",
                "gcc64-debug",
                "--target",
                "clean"
            ],
            "options": {
                "cwd": "${workspaceFolder}/code"
            },
            "problemMatcher": []
        }
    ]
}
